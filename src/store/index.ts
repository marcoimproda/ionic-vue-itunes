import { createStore } from 'vuex';
import { search } from "./modules/search.store";

export const store = createStore({
    state: {},
    mutations: {},
    actions: {},
    modules: {
        search
    },
})